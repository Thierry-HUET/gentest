services:
  anonyx-gen:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./src:/app/src
      - ./static:/app/static
      - ./.streamlit:/app/.streamlit
    environment:
      - STREAMLIT_BROWSER_GATHER_USAGE_STATS=false
      - PYTHONPATH=/app/src
